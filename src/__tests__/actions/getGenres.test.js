import * as actions from "../../actions/getGenres";
describe("Test getGenres action", () => {
  const GENRES = "GENRES";
  it("should create an action to fetch genres successfully", () => {
    // const expectedGetGenresSuccessfully = {
    //   type: GENRES,
    //   payload: "genres fetched successfully"
    // };
    // expect(actions.genres("genres fetched successfully")).toEqual(
    //   expectedGetGenresSuccessfully
    // );
  });
});
